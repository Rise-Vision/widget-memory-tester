test:
  override:
    - echo "There is no test"
  deployment:
    staging:
      branch: /(feature|fix|chore).*/
      commands:
        - echo "Do nothing for staging"
    production:
      branch: master
      commands:
        - ssh widget-memory-tester@104.197.26.57 'sudo /usr/share/google/run-startup-scripts'
